# 安全策略

## 支持范围

只维护最新发布版本。桌面客户端本身不处理网络请求，也不存储凭据：它只负责启动本地
Harness 服务并把界面显示出来。Harness 服务的安全性由上游项目负责。

## 报告漏洞

请**不要**用公开 issue 报告安全问题，改用 GitHub 的私密漏洞报告
（仓库 **Security → Report a vulnerability**）。

请附上：

- 受影响的版本
- 复现步骤或最小验证
- 影响范围（能否导致本地代码执行、凭据泄露、越权访问等）

## 本项目相关的安全边界

改动以下行为时请格外谨慎，并在 PR 中说明理由：

- 服务的绑定地址（当前只允许回环 `127.0.0.1`）
- 启动令牌的处理（令牌只用于换取会话 Cookie，不得写入日志或磁盘）
- 窗口的 `webPreferences`（当前 `contextIsolation: true`、`sandbox: true`、
  `nodeIntegration: false`）
- 外部链接的处理（当前一律交给系统默认浏览器，不在应用内打开）
- 子进程的启动参数（不得引入 shell 解释执行）

## 已知的非安全问题

- **未签名的安装包。** 发布产物没有代码签名证书，Windows SmartScreen 可能提示
  「未知发布者」。这是预期行为，不是漏洞。
- **Harness 服务可被本机其它进程访问。** 与命令行 `dsh web` 的行为一致。
